// src/app/models/dto/admin/update-user-roles.request.dto.ts
// Conceptual DTO for updating a user's roles
export interface UpdateUserRolesRequestDto {
  roles: string[]; // The new set of roles for the user
}
